bot:
  protocol: "ws"  # 协议 ws/wss
  host: "192.168.100.249"  # 主机地址
  port: 3001  # 端口号
  accessToken: "fenglin666"  # 访问令牌
  throwPromise: true
  reconnection: # 重连配置
    enabled: true
    attempts: 10
    delay: 5000
cmd:
 prefix: "#"
